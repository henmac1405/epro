import SwiftUI

struct VersionView: View {
    var body: some View {
        VStack {
            Text("Bukan Versi terbaru, segera update aplikasi anda")
                .font(.title)
        }
        
    }
}
